// import { shallow } from "enzyme";
// import HomeScreen from "../../../components/09-useContext/HomeScreen";
// import { UserContext } from "../../../components/09-useContext/UserContext";


describe('Pruebas en HomeScreen component', () => {
    
    /* Debería usarse la funcion mount de enzyme, pero no está soportada en
    la version 17 de React, lo mismo con LoginScreen, AppRouter...


    // el shallow solo renderiza el primer componente, el padre en caso de haber varios
    // el hijo no lo renderizara en este caso, solo mostrara literalmente <HomeScreen />

    const wrapper = shallow(
        <UserContext.Provider value={{name: 'jose', email: 'jose@test.com'}}>
        <HomeScreen />
        </UserContext.Provider>
    )
    
    test('debe mostrarse correctamente', () => {
        expect(wrapper).toMatchSnapshot()
    })
    */
        
    test('dummy test', () => {
        
    })
    
    
})
